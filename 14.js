let string = "diwakar n s";
console.log(string,",","length of string is:",string.length,",",string[0],",",string[string.length-1]);

// TO ACCESS LAST CHARACTER IN STRING IN JS USE
         // stringName[stringName.length-1] //  string name(can be any user defined one).

    // TO ACCESS CHARACTERS IN JS USE
         // stringName[];