// STING METHODS .
 // WON`T CHANGE THE ORIGINAL STRING BUT CREATES A NEW VALUE AND RETURNS IT.
 // UPPER CASE CONVERTION.
 // stringname.toUpperCase() - > function code.
 //strings are immutable.

//  let string = "We are competition";
//  console.log(string.toUpperCase());

 // LOWER CASE CONVERTION.
  // stringname.toLowerCase();
let str = "WE ARE COMPETITION";
str = str.toLowerCase();
console.log(str);

// TO REMOVE WHITESPACES ->stringname.trim()
let string = "      we are competition            "
console.log(string.trim());